import React from 'react';

const Management = (props) => <div></div>;

export default Management;

// https://www.npmjs.com/package/react-image-crop

// <input value={props.row.values.name} onBlur={() => clicked(props.row.values)} />
